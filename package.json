{
  "name": "mood",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix apps/web run dev",
    "build": "npm --prefix apps/web run install:ci && npm --prefix apps/web run build",
    "test": "npm --prefix apps/web run test",
    "lint": "npm --prefix apps/web run lint"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
